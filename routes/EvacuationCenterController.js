const express = require("express");
const router = express.Router();
const evacuationController = require("../controllers/EvacuationCenterController");
const upload = require("../middlewares/uploads");

module.exports = router;
